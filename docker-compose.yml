services:
  auth-server:
    build: .
    ports:
      - "8765:8765"
    environment:
      - DATABASE_URL=sqlite:///app/hw_genie.db
    volumes:
      - ./hw_genie.db:/app/hw_genie.db
    environment:
      - HW_GENIE_AUTH_PORT=8765
